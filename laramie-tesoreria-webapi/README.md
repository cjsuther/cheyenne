# laramie-tesoreria-webapi
Webapi de tesorería (proyecto Laramie)

# configuración:
# dar de alta usuario en rabbit desde la consola:
# rabbitmqctl add_user <username> <password>
# rabbitmqctl set_permissions -p / <username> .* .* .*
