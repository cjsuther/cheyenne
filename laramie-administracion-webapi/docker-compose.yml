version: "3.9"

services:
  major-laramie-administracion-webapi:
    #container_name: major-laramie-administracion-webapi
    image: major-laramie-administracion-webapi:v1
    build:
      dockerfile: 'Dockerfile'
    command: "npm run start-prod"

    volumes:
      - .:/app
      - /app/node_modules
      - major-laramie-data_files:/var/lib/major-laramie-repository

    ports:
      - "3012:3012"

    networks:
      - major-laramie

volumes:
  major-laramie-data_files:
    external: true

networks: 
  major-laramie:
    name: major-laramie
    driver: bridge